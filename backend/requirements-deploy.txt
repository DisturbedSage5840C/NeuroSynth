# Slim API runtime for free-tier hosting (Render/Fly/Railway).
# Excludes torch & shap (too large for free build/disk limits) — the app's lifespan
# loads those lazily and degrades gracefully when absent, so all HTTP endpoints still
# respond (auth, feature schema, reports with LLM/template fallback). For full
# multi-modal ML inference, deploy the Docker image (docker-compose) on a paid host.
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.4
pydantic-settings==2.3.4
structlog==24.4.0
tenacity==8.5.0
aiofiles==24.1.0
httpx==0.27.0
asyncpg==0.29.0
redis==5.0.7
celery==5.4.0
slowapi==0.1.9
pyjwt==2.8.0
python-jose==3.3.0
pandera==0.20.2
prometheus-client==0.20.0
prometheus-fastapi-instrumentator==7.0.0
pandas==2.2.0
numpy==1.26.4
scikit-learn==1.4.0
scipy==1.12.0
joblib==1.3.2
requests==2.31.0
python-multipart==0.0.9
jinja2==3.1.4
anthropic>=0.39.0,<1.0
# Optional: pull trained model artifacts from Cloudflare R2 on startup (no-op if
# R2_* env vars are unset). Small, safe to include in the slim runtime.
boto3>=1.34,<2.0
