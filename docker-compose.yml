version: '3.8'
services:
  neurosynth:
    build: .
    ports: ["8000:8000"]
    environment:
      - HF_TOKEN=${HF_TOKEN}
    volumes:
      - ./oasis_longitudinal.csv:/app/oasis_longitudinal.csv
      - ./models:/app/models
