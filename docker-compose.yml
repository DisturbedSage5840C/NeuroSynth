version: '3.8'
services:
  neurosynth:
    build: .
    ports: ["8000:8000"]
    env_file:
      - .env
    environment:
      - HF_TOKEN=${HF_TOKEN}
    volumes:
      - ./alzheimers_disease_data.csv:/app/alzheimers_disease_data.csv
      - ./models:/app/models
